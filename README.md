# transfer-plugins
[!WARNING]
ПЛАГИНЫ ПОСЛЕ ПЕРЕНОСА С 99% ШАНСОМ НЕ РАБОТАЮТ СКРИПТ ТЕСТОВЫЙ
!!! НЕ РЕКОМЕНДУЕТСЯ ИСПОЛЬЗОВАТЬ ЕГО В ТОМ ВИДЕ В КОТОРОМ ОН СЕЙЧАС ЕСТЬ!!!

скрипт для виндовс 11 который переносит все плагины и скрипты афтер эффектс и любых давок буду расширять функционал добавлять пресеты выборки и т д пока это лично для моего пк
самое главное это можно сказать все выше описанное становится portable и переносится на другой компьютер спокойно но из-за симлинков ничего не сбивает т.к. система считает что все на диске с системой
+ освобождается диск C
# README.md – Перенос плагинов, скриптов и настроек с диска C: на D:

Этот PowerShell-скрипт автоматически переносит все ваши плагины, скрипты, настройки и пользовательские данные с системного диска `C:` на диск `D:`, создавая **символические ссылки (Junction)** на исходных местах.  
После переноса программы «видят» папки как прежде, но физически они хранятся на `D:`, экономя место на системном диске.

---

## 📦 Что переносится

- **Звуковые плагины** – VST2, VST3, CLAP, отдельные бренды (FabFilter, iZotope, Native Instruments, Waves, Valhalla и др.)
- **Видеоплагины** – OFX, Adobe Plug-ins, BorisFX, Red Giant, REVision, ProductionCrate, Maxon и др.
- **Плагины After Effects** – все `.aex` плагины из папки `Plug-ins`
- **Скрипты и расширения** – папки `Scripts`, `ScriptUI Panels`, CEP-расширения, UXP, Startup Scripts
- **Системные данные** – `ProgramData`, `AppData\Roaming`, `AppData\Local`, `Documents`
- **Общедоступные папки** – `C:\Users\Public\Documents` и папки популярных брендов (Waves, Native Instruments, Soundtoys)
- **Реестр** – сохраняются ключи активаций и настроек в `D:\RESTORE\Registry_Keys`

---

## ⚙️ Требования

- Windows 10/11 (64‑бит)
- PowerShell запущен **от имени администратора**
- Диск `D:` должен существовать и иметь достаточно свободного места
- Все программы (After Effects, DAW, Creative Cloud) **должны быть закрыты** перед запуском

---

## 🚀 Как использовать

1. **Скачайте** файл `mega_final.ps1` на свой компьютер.
2. **Щёлкните правой кнопкой** по файлу и выберите *«Запустить с помощью PowerShell»* (или откройте PowerShell от администратора и выполните `.\mega_final.ps1`).
3. Дождитесь завершения – скрипт покажет зелёное сообщение об успехе.
4. **Перезагрузите компьютер**, чтобы все симлинки и изменения реестра применились.

> **Важно!** Перед запуском убедитесь, что все нужные папки на `C:` действительно существуют – скрипт их перенесёт и создаст симлинки.

---

## 🗂️ Результат

После выполнения вы получите:

- Все файлы плагинов и скриптов на диске `D:` в структурированном виде:
  - `D:\Files For All\Documents\Plugins For All\Sound Plugins\`
  - `D:\Files For All\Documents\Plugins For All\Video Plugins\`
  - `D:\Files For All\Documents\Scripts For All\Video Scripts\`
- Резервную копию ключей реестра в `D:\RESTORE\Registry_Keys`
- Восстановительный скрипт `D:\RESTORE\restore.ps1`

---

## 🔁 Восстановление на новой системе (или после переустановки Windows)

Если вы переустановили Windows или хотите развернуть все плагины на новом компьютере:

1. **Установите** сами программы (After Effects, DAW и т.д.) в те же пути (например, на `D:` или `C:` – как было изначально).
2. Убедитесь, что папка `D:\Files For All\...` с плагинами и папка `D:\RESTORE` сохранены (они должны быть на месте).
3. Откройте PowerShell от имени администратора.
4. Перейдите в папку `D:\RESTORE` и запустите `.\restore.ps1`.

**Что произойдёт:**
- Скрипт **импортирует** сохранённые ключи реестра (активации, пресеты).
- **Создаст** все симлинки (Junction) на месте исходных папок (например, `C:\Program Files\Common Files\VST3` будет указывать на `D:\...\Sound Plugins\CommonFiles_VST3`).
- После перезагрузки все ваши плагины и скрипты будут доступны программам так, будто они никогда никуда не переезжали.

> ⚠️ **Важно:** Этот скрипт **не перемещает** файлы обратно на `C:`. Он только **привязывает** свежую систему к данным на `D:`. Если вам нужен буквальный «откат» (переместить всё обратно на `C:`), такой функционал в этом скрипте не предусмотрен – для этого нужно отдельное решение.

---

## 📋 Аудит и доработка

После первого запуска скрипт создаёт лог `D:\RESTORE\unmapped_folders.log`. В нём перечислены все папки на `C:`, которые **не попали** в список переноса.  
Если вы видите там нужные вам каталоги (например, `C:\Program Files\SomeVendor`), вы можете:

- Добавить соответствующую строку в массив `$Mapping` в скрипте
- Запустить скрипт повторно – он обработает только новые папки, не трогая уже созданные симлинки

---

## ⚠️ Предупреждения

- **Всегда делайте бэкап** важных данных перед запуском.
- Скрипт **не перемещает** сами исполняемые файлы программ (After Effects, DAW) – только их дополнительные данные.
- Если какие‑то файлы заблокированы процессами, скрипт сообщит об этом и продолжит работу.
- Для корректной работы всех плагинов может потребоваться **перезагрузка** после переноса.

---

## 📄 Лицензия

Скрипт предоставляется «как есть». Автор не несёт ответственности за возможные потери данных. Используйте на свой страх и риск.

---

## 🤝 Вклад

Если вы нашли баг или хотите дополнить список переносимых папок – создавайте Issue или Pull Request.

---

## ❓ Как запустить?

### ✅ Пошаговая инструкция

1. **Закрой все программы** (After Effects, Creative Cloud, DAW).

2. **Открой PowerShell от имени администратора**:
   - Нажми `Win + X` → выбери **«Терминал (администратор)»** или **«Windows PowerShell (администратор)»**.

3. **Перейди в папку со скриптом**:
   пример:
   ```powershell
   cd "D:\Desktop"
   ```

4. **Разреши выполнение скриптов** (если ещё не сделано):
   ```powershell
   Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process
   ```

5. **Запусти скрипт**:
   ```powershell
   .\mega_final.ps1
   ```

---

### 🔄 Что будет, если запустить повторно

- **Уже существующие симлинки** будут пропущены (не пересоздадутся).
- **Новые папки**, которые не были обработаны в прошлый раз, будут перенесены.
- **Реестр** будет дополнен новыми ключами (если они появились).

Всё безопасно – можно запускать несколько раз.

---


**Удачи с переносом!** 🚀
